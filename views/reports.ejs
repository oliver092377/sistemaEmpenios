<%- include('partials/header', {title: 'Flujo de Caja - Sistema de Empeños', currentPage: 'reports'}) %>

<%- include('partials/sidebar', {currentPage: 'reports'}) %>

<div class="main-content">
    <!-- Page Heading -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 text-gray-800">Reportes</h1>
        <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addTransactionModal">
            <i class="fas fa-plus me-2"></i>Nueva Transacción
        </button>
    </div>

    <!-- Contenido específico de la página de flujo de caja -->
    <div class="row">
        <div class="col-lg-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3 d-flex justify-content-between align-items-center">
                    <h6 class="m-0 font-weight-bold text-primary">Registrar Transacción</h6>
                </div>
                <div class="card-body">
                    <!-- Formulario de transacción -->
                    <form>
                        <!-- Campos del formulario -->
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Más contenido específico -->
</div>

<%- include('partials/footer') %>

<%- include('partials/scripts', {currentPage: 'cashflow'}) %>
    <script>
        // Scripts específicos para la página de flujo de caja
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Página de flujo de caja cargada');
            // Aquí tu código JavaScript específico para esta página
        });
        console.log("funciona");
    </script>
</body>
</html>